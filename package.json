{
  "name": "root",
  "private": true,
  "workspaces": [
    "packages/*"
  ],
  "scripts": {
    "build:apps": "npx lerna run build",
    "start:example": "npm run build:apps && npx lerna run dev --scope example_repo"
  },
  "dependencies": {},
  "devDependencies": {
    "lerna": "^8.1.2"
  }
}
